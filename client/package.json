{
  "name": "client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "presprout": "node scripts/build-sprout-manifest.mjs",
    "predev": "npm run presprout",
    "prebuild": "npm run presprout",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.87.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "fast-xml-parser": "^4.5.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "jsdom": "^25.0.1",
    "vite": "^6.0.7",
    "vitest": "^3.0.4"
  }
}
